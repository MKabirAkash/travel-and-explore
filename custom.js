$(document).ready(function() {
    $('.add_cmnt_btn').click(function (e){
       /* e.preventDefault();
        var msg=$('.comment_textbox').val();
        if($.trim(msg).length ==0 ){
            error_msg="please type a comment";
            $('.error_status').txt(error_msg);
        }
        else{
            error_msg="";
            $('.error_status').txt(error_msg);
        }
        if(error_msg != ''){
            return false;
        }
        else{

        }*/
        alert('linked');

    });
});